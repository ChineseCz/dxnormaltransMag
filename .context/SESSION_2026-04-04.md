# 会话上下文归档（最新：2026-04-05）

> 新会话开始时，请先阅读本文件恢复上下文，再继续工作。

---

## 一、项目基本信息

- **项目名**：面向网络安全的智能化物理场预测平台（dx-platform）
- **根目录**：`E:\Project\dxnormaltransMag\dxnormaltransMag\`
- **技术栈**：FastAPI(Python) 后端 + Vue3 前端 + **PostgreSQL** + Redis + Docker

---

## 二、历次会话已完成工作

### 2.1 P1-1：JWT 替换 SHA256 token ✅（2026-04-04）

| 内容 | 文件 |
|------|------|
| 新建 JWT 工具模块（签发/验签/黑名单/Depends） | `backend/api/auth/jwt.py` |
| 登录接口改为返回 JWT | `backend/api/auth/routes.py` |
| 新增 `POST /api/user/logout` — jti 加黑名单 | `backend/api/auth/routes.py` |
| 新增 `POST /api/user/refresh` — 旧 token 吊销 + 签发新 token | `backend/api/auth/routes.py` |
| 新增 `GET /api/user/me` — 验签并返回当前用户信息 | `backend/api/auth/routes.py` |
| 修复 `data.py` 顶层 import 崩溃（懒加载 + Stub 兜底） | `backend/api/data/routes.py` |

### 2.2 MySQL → PostgreSQL 迁移 ✅（2026-04-04）

| 变更 | 详情 |
|------|------|
| 新建 `backend/db_pg.py` | psycopg2 + DBUtils 连接池，读取 `PG_*` 环境变量 |
| 新建 `backend/db_setup_pg.py` | 写入 bcrypt 用户（幂等，重复执行安全）|
| 路由完全切换至 PostgreSQL | `backend/api/auth/routes.py` |
| DDL 初始化脚本 | `sql/pg/01_init_schema.sql` |

### 2.3 Docker 环境统一 & 数据库初始化 ✅（2026-04-05）

| 操作 | 结果 |
|------|------|
| 所有容器从不同网络迁移到统一 `env_my-network` | pgAdmin ↔ postgres 容器名解析正常 |
| 修复 `env/sql/pg/` 目录为空问题 | 已同步 `01_init_schema.sql` 到该目录 |
| 手动执行 DDL + 初始化用户 | 3 个角色、3 个初始用户写入完成 |
| 端到端验证注册/登录/me/logout/黑名单 | 全部 ✅ |

### 2.4 端到端验证结果（2026-04-05 最新）

```
✅ POST /api/user/register   → 注册成功，自动返回 JWT
✅ POST /api/user/login      → JWT bearer，role=管理员（中文正确）
✅ GET  /api/user/me         → user_id=1, username=admin
✅ POST /api/user/logout     → code=200，token 加入 Redis 黑名单
✅ 旧 token 访问 /me         → 401 "Token 已注销，请重新登录"
```

### 2.5 技术细节

- **黑名单后端**：Redis 优先（`redis://localhost:6379/0`），降级内存字典
- **PostgreSQL 连接**：`127.0.0.1:5432`，用户 `dx_user`，库 `dx_platform_db`
- **JWT 算法**：HS256，JTI = UUID4，默认有效期 86400s
- **角色体系**：`role_id=1` 超级管理员 / `role_id=2` 工程师 / `role_id=3` 访客
- **PowerShell 注意**：终端中文不显示，但 JWT payload 实际值正确（已 Python 解码验证验证）

---

## 三、当前所有 API 模块状态

| 文件 | 框架 | 功能状态 |
|------|------|---------|
| `api/auth/jwt.py` | FastAPI ✅ | JWT 工具（create/decode/revoke/Depends）|
| `api/auth/routes.py` | FastAPI ✅ | bcrypt 登录 + JWT + register + logout + refresh + me（**PostgreSQL**）|
| `api/data/routes.py` | FastAPI ✅ | 文件上传 + 预处理（preprocess 缺失时 Stub 兜底）|
| `api/data/dataset.py` | FastAPI ✅ | 完整 CRUD + 文件管理 + 四步流水线 |
| `api/ml/model.py` | FastAPI ✅ | 模型列表（train 为 **Stub**）|
| `api/ml/predict.py` | FastAPI ✅ | 场数据可视化（realtime 为 **Stub**）|
| `api/assistant/routes.py` | FastAPI ✅ | Mock 答案（**未接真实 LLM**）|
| `api/devices/gaoya.py` | FastAPI ✅ | 高压套管真实数据 |
| `api/devices/reactor.py` | FastAPI ✅ | 电抗器真实数据 |
| `api/devices/transfield.py` | FastAPI ✅ | 变压器电场真实数据 |

---

## 四、启动命令

```powershell
# 1. 启动 Docker 环境（从 env/ 目录）
cd E:\Project\dxnormaltransMag\dxnormaltransMag\env
docker-compose up -d

# 2. 初始化用户（首次或重建数据库后）
cd E:\Project\dxnormaltransMag\dxnormaltransMag
python backend/db_setup_pg.py

# 3. 启动后端服务
python run.py
# Swagger UI: http://localhost:5000/docs
```

---

## 五、后续待完成工作（优先级排序）

### 🔶 P1 — 核心功能补全

| # | 任务 | 文件 |
|---|------|------|
| ~~1~~ | ~~JWT 替换 SHA256 token~~ | ✅ 已完成 |
| ~~-~~ | ~~MySQL → PostgreSQL 迁移~~ | ✅ 已完成 |
| ~~-~~ | ~~Docker 网络统一 + DB 初始化~~ | ✅ 已完成 |
| 2 | **真实模型训练接入**：调用 `core_algorithms/dl_experiment/` 脚本（DNN train） | `backend/api/ml/model.py` |
| 3 | **真实 PyTorch 推理链路**：Z-Score → 推理 → 逆PCA → 返回场值 | `backend/api/ml/predict.py` |
| 4 | **Qwen API 流式接入**：SSE 逐字推送 + 对话历史持久化 | `backend/api/assistant/routes.py` |

### 🔵 P2 — 论文实验收尾

| # | 任务 | 文件 |
|---|------|------|
| 5 | 修复 `judge_faithfulness` 的 `__wrapped__` bug | `core_algorithms/rag_experiment/` |
| 6 | 运行 RAGAS 实验，生成 `experiment_result.xlsx` | `core_algorithms/rag_experiment/` |

---

## 六、Docker 环境速查

```
PostgreSQL : localhost:5432        (dx_user/dx123456  db=dx_platform_db)  ✅
pgAdmin    : http://localhost:5050  (admin@dx.com / admin123)              ✅
Redis      : localhost:6379         ✅ JWT 黑名单
MySQL      : localhost:13306        (root/123456)
Swagger UI : http://localhost:5000/docs
```

**pgAdmin 连接 PostgreSQL 参数**：
- Host: `postgres`（容器名，同 `env_my-network`）
- Port: `5432`
- Database: `dx_platform_db`
- Username: `dx_user`
- Password: `dx123456`

---

## 七、新会话推荐提示词

> ⚠️ 最新进度已归档到 `.context/SESSION_2026-04-05.md`，请优先读取那个文件。

```
我在开发 dx-platform 项目（面向电力装备的智能化物理场预测平台）。
请先读取 .context/SESSION_2026-04-05.md 了解最新进度，
然后继续执行 P1-2：真实模型训练接入（backend/api/ml/model.py）。
项目根目录：E:\Project\dxnormaltransMag\dxnormaltransMag
```
