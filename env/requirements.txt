fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # FastAPI 文件上传必需
python-dotenv>=1.0.0         # .env 文件加载

# ── 数据库 ────────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9        # PostgreSQL 驱动
DBUtils>=3.0.3                # 连接池（PooledDB）

# ── 鉴权 ──────────────────────────────────────────────────────────────────────
bcrypt>=4.1.2
PyJWT>=2.8.0

# ── 数据科学 ──────────────────────────────────────────────────────────────────
numpy>=1.24
scikit-learn>=1.4
# torch>=2.0.0               # GPU 推理用，按需手动安装（体积大）

# ── 异步任务 ──────────────────────────────────────────────────────────────────
celery>=5.3.6
redis>=5.0.3

# ── AI / LLM ──────────────────────────────────────────────────────────────────
dashscope>=1.14.0             # Qwen API
# langchain>=0.2.0            # RAG 链路（按需开启）
# faiss-cpu>=1.8.0            # 向量库（按需开启）

# ── 工具 ──────────────────────────────────────────────────────────────────────
pydantic>=2.6.0
requests>=2.31.0
